kind = "web"
previewPath = "/"
title = "XScore"
version = "1.0.0"
id = "artifacts/xscore"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21986

[services.development]
run = "pnpm --filter @workspace/xscore run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/xscore", "run", "build" ]
publicDir = "artifacts/xscore/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21986"
BASE_PATH = "/"
